export const environment = {
    production: false,
    // En desarrollo usamos URL absoluta para evitar choques de rutas SPA (p. ej. /admin/*, /contacto)
    apiBaseUrl: 'http://localhost:8083'
  };
  
